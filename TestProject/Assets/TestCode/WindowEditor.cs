using System.Collections;
using System.Collections.Generic;
using GameSystem;
using GameSystem.Ui;
using UnityEngine;

public class WindowEditor : MonoBehaviour
{




    // Use this for initialization
    void Start()
    {

    }



    // Update is called once per frame
    void Update()
    {

        if (Input.GetKeyUp(KeyCode.Q))
        {
            //ShowWindowData showWindowData = new ShowWindowData();

            //WindowDataRelationMap data = new WindowDataRelationMap();

            //List<RealtionRunData> realtionRunDatas=new List<RealtionRunData>();
            //data.Datas = realtionRunDatas;
            //showWindowData.contextData =data;

            //UICenterMasterManager.Instance.ShowWindow(WindowID.RelationMapWindow,showWindowData);

        }

    }
}
